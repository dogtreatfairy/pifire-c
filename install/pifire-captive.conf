# Installed to /etc/NetworkManager/dnsmasq-shared.d/pifire-captive.conf
# While the PiFire setup hotspot is active (NetworkManager "shared" mode), answer every DNS
# name with the hotspot address so phones open the captive setup page.
address=/#/10.42.0.1
